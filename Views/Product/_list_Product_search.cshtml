@model IEnumerable<QuaMonBa_Store.ViewComponentModels.HangHoaVM>
<a> Search For : @ViewBag.TuKhoa </a>
@foreach (var item in Model)
{
    <div class="col-md-6 col-lg-4 wow fadeIn" data-wow-delay="0.1s">
        <div class="product-item text-center border h-100 p-4 position-relative">

            <img class="img-fluid mb-4" src="~/img/shampoo.png" alt="">

            <div class="position-absolute bg-warning text-white px-3 py-1 rounded" style="top: 10px; left: 10px;">
                @item.TenLoai
            </div>

            <div class="mb-2">
                <small class="fa fa-star text-primary"></small>
                <small class="fa fa-star text-primary"></small>
                <small class="fa fa-star text-primary"></small>
                <small class="fa fa-star text-primary"></small>
                <small class="fa fa-star text-primary"></small>
                <small>(99)</small>
            </div>
            <a href="" class="h6 d-inline-block mb-2">@item.TenHH</a>
            <h5 class="text-primary mb-3">@item.DonGia VNĐ</h5>
         @*    @if (User.IsInRole("Admin"))
            {
                <a asp-controller="HangHoa" asp-action="Create" class="btn btn-success mb-3">
                    <i class="fas fa-plus"></i> Thêm sản phẩm mới
                </a>
            } *@

            @if (User.IsInRole("Admin"))
            {
                <div class="btn-group">
                    <!-- Đổi href thành type="button" cho chuẩn HTML -->
                    <button type="button" class="btn btn-warning" onclick="moFormSua(@item.MaHH)">
                        <i class="fas fa-edit"></i> Sửa
                    </button>

                    <!-- Đổi <button> thành <a> -->
                    <a asp-controller="Product" asp-action="Delete" asp-route-id="@item.MaHH"
                       class="btn btn-danger"
                       onclick="return confirm('Bạn có chắc chắn muốn xóa?')">
                        <i class="fas fa-trash"></i> Xóa
                    </a>
                </div>
            }
            else
            {
                <a asp-controller="Shopcart" asp-action="AddToCart" asp-route-id="@item.MaHH" class="btn btn-outline-primary px-3">
                    <i class="fas fa-shopping-cart"></i> Add To Cart
                </a>
            }
        </div>
    </div>
}